/***
 * Enum for Interaction's Contexts Types
 * @readonly
 * @enum {number}
 * */
const InteractionContextType = {
  Guild: 0,
  BotDm: 1,
  PrivateChannel: 2,
};

/***
 * Enum for Interaction Integration Types
 * @readonly
 * @enum {number}
 * */
const InteractionIntegrationType = {
  GuildInstall: 0,
  UserInstall: 1,
};
